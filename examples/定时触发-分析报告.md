# 工作流: 定时触发
> Process ID: `6944f290063a4bbf0780bebf` | 节点数: 7 | 深度: 0

## 触发方式
- 名称: 定时触发

## 完整执行逻辑

### 1. [触发] 定时触发

### 2. [获取多条] 查询工作表
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 操作: 获取多条(全部)
- 筛选条件:
  - 业务类型 属于(in) "外贸"
- 排序:
  - 挂售校准时间 升序
- 限制条数: 100

### 3. [子流程调用] 子流程
- 调用: 「未命名子流程」(`694796196436ebbd45a77437`)
- 数据源: 「查询工作表」


---

# 子流程: 未命名子流程
> Process ID: `694796196436ebbd45a77437` | 节点数: 36 | 深度: 1

## 完整执行逻辑

### 1. [触发] 子流程
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 数据源: 「子流程」
- 输入参数: 校准系统挂售中-外贸

### 2. [获取单条] 代理
- 工作表: 芝麻代理 (`63edb8ac1c190a21fe376ad3`)
- 操作: 查找(按筛选器)
- 筛选条件:
  - 国家 属于(in) "国外" AND snk价格请求状态 属于(in) "正常"

### 3. [获取单条] 查询工作表
- 工作表: 比价数据 (`6290caed1e00e6d0665284c4`)
- 操作: 查找(按筛选器)
- 筛选条件:
  - global_sku_id 属于(in) [子流程].global_sku_id

### 4. [条件互斥分支] 

#### 分支 1 (满足条件)

### 1. [并行分支] 

#### 分支 1 
- **条件**: [查询工作表].snk_id 大于等于(>=) (空) OR [查询工作表].snk_id 等于(=) "0"

### 1. [查询外部表] 鞋子
- 工作表: 单尺码列表-非账户 (`6319db7e777f0d0aea396441`)
- 字段映射:
  - `货号` ← [查询工作表].得物货号 (type:2)
  - `尺码编号` ← [查询工作表].size_number/size_id (type:2)
  - `637ae3eebb225143d6f7dbf3` ← [代理].IP (type:2)
  - `637ae3eebb225143d6f7dbf4` ← [代理].port (type:2)

### 2. [并行分支] 分支

#### 分支 1 
- **条件**: [鞋子].status 等于(=) "success"

### 1. [获取多条] 获取数组对象
- 操作: 获取数组对象
- 数据源: 「鞋子」
- 字段映射:
  - `631df1ba777f0d0aeae1f62c` ←  (type:0)

### 2. [获取数据条数] 获取数据条数
- 操作: 获取条数
- 数据源: 「获取数组对象」

### 3. [并行分支] 分支

#### 分支 1 
- **条件**: [获取数据条数].结果 不在范围内 "0"

### 1. [更新记录] 更新记录
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 操作: 更新
- 数据源: 「子流程」
- 字段映射:
  - `jzsj` ← [系统].当前时间 (type:16)

#### 分支 2 

### 1. [更新记录] 删除记录
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 操作: 删除
- 数据源: 「子流程」

#### 分支 2 

_(无后续节点)_

#### 分支 2 

### 1. [查询外部表] 衣服
- 工作表: 衣服单尺码列表数据 (`646b7ff06ffb7551424aceba`)
- 字段映射:
  - `6319dbba777f0d0aea39646c` ← [查询工作表].snk_id (type:2)
  - `6319dbba777f0d0aea39646d` ← [查询工作表].size_number/size_id (type:2)
  - `637ae3eebb225143d6f7dbf3` ← [代理].IP (type:2)
  - `637ae3eebb225143d6f7dbf4` ← [代理].port (type:2)

### 2. [并行分支] 分支

#### 分支 1 
- **条件**: [衣服].status 等于(=) "success"

### 1. [获取多条] 获取数组对象
- 操作: 获取数组对象
- 数据源: 「衣服」
- 字段映射:
  - `646b80ff6ffb7551424bc5a2` ←  (type:0)

### 2. [获取数据条数] 获取数据条数
- 操作: 获取条数
- 数据源: 「获取数组对象」

### 3. [并行分支] 分支

#### 分支 1 
- **条件**: [获取数据条数].结果 不在范围内 "0"

### 1. [更新记录] 更新记录
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 操作: 更新
- 数据源: 「子流程」
- 字段映射:
  - `jzsj` ← [系统].当前时间 (type:16)

#### 分支 2 

### 1. [更新记录] 删除记录
- 工作表: 挂售表-新逻辑 (`6937d2e8be2ffd8cd2532a58`)
- 操作: 删除
- 数据源: 「子流程」

#### 分支 2 

_(无后续节点)_

#### 分支 2 (其他情况(else))

### 1. [站内通知] 发送站内通知
- 通知对象: 张磊, 李义, 李聪


## 调用关系
```
定时触发 (7节点)
└── 未命名子流程 (36节点)
```

---

# 诊断报告

共发现 **12** 个问题：高=4 中=3 低=5

| # | 严重度 | 类别 | 问题 | 预期收益 |
|---|--------|------|------|----------|
| 1 | 🔴 高 | 重复查询 | 「芝麻代理」在子流程中被重复查询 | 减少 99 次数据库查询 |
| 2 | 🔴 高 | 重复查询 | 「单尺码列表-非账户」在子流程中被重复查询 | 减少 99 次数据库查询 |
| 3 | 🔴 高 | 重复查询 | 「衣服单尺码列表数据」在子流程中被重复查询 | 减少 99 次数据库查询 |
| 4 | 🔴 高 | 冗余API调用 | 并行分支中同时调用多个外部API（鞋子、衣服） | 减少约50%的外部API调用 |
| 5 | 🟡 中 | 删除安全 | 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录 | 避免因临时故障导致的数据丢失 |
| 6 | 🟡 中 | 删除安全 | 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录 | 避免因临时故障导致的数据丢失 |
| 7 | 🟡 中 | 异常处理 | 2个外部API/表调用无重试机制 | 提高校准/处理成功率 |
| 8 | 🔵 低 | 异常处理 | 分支「分支2」无后续处理 | 便于排查问题 |
| 9 | 🔵 低 | 异常处理 | 分支「分支2」无后续处理 | 便于排查问题 |
| 10 | 🔵 低 | 可维护性 | 子流程未命名（694796196436ebbd45a77437） | 提高可维护性 |
| 11 | 🔵 低 | 可维护性 | 子流程「未命名子流程」节点数过多（36） | 降低维护难度 |
| 12 | 🔵 低 | 性能 | 批次量为 100，需结合触发频率评估 | 确保全量数据及时得到处理 |

## 🔴 问题 1: 「芝麻代理」在子流程中被重复查询

- **类别**: 重复查询
- **现状**: 每条记录进入子流程都会查询一次「芝麻代理」（筛选条件不依赖当前记录），100条记录=100次相同查询。
- **建议**: 将「芝麻代理」的查询上移到主流程（子流程之前），通过参数传入子流程。
- **预期收益**: 减少 99 次数据库查询

## 🔴 问题 2: 「单尺码列表-非账户」在子流程中被重复查询

- **类别**: 重复查询
- **现状**: 每条记录进入子流程都会查询一次「单尺码列表-非账户」（筛选条件不依赖当前记录），100条记录=100次相同查询。
- **建议**: 将「单尺码列表-非账户」的查询上移到主流程（子流程之前），通过参数传入子流程。
- **预期收益**: 减少 99 次数据库查询

## 🔴 问题 3: 「衣服单尺码列表数据」在子流程中被重复查询

- **类别**: 重复查询
- **现状**: 每条记录进入子流程都会查询一次「衣服单尺码列表数据」（筛选条件不依赖当前记录），100条记录=100次相同查询。
- **建议**: 将「衣服单尺码列表数据」的查询上移到主流程（子流程之前），通过参数传入子流程。
- **预期收益**: 减少 99 次数据库查询

## 🔴 问题 4: 并行分支中同时调用多个外部API（鞋子、衣服）

- **类别**: 冗余API调用
- **现状**: 并行分支同时执行所有路径，但如果数据只属于其中一个类别，则另一个调用完全浪费。
- **建议**: 增加品类/类型判断条件，改为条件互斥分支，只调用匹配的API。
- **预期收益**: 减少约50%的外部API调用

## 🟡 问题 5: 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录

- **类别**: 删除安全
- **现状**: 删除操作不可逆。如果上游条件判断错误（如外部API临时故障返回空数据），可能导致误删。
- **建议**: 改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。
- **预期收益**: 避免因临时故障导致的数据丢失

## 🟡 问题 6: 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录

- **类别**: 删除安全
- **现状**: 删除操作不可逆。如果上游条件判断错误（如外部API临时故障返回空数据），可能导致误删。
- **建议**: 改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。
- **预期收益**: 避免因临时故障导致的数据丢失

## 🟡 问题 7: 2个外部API/表调用无重试机制

- **类别**: 异常处理
- **现状**: 外部调用（鞋子、衣服）失败后直接跳过，无重试。
- **建议**: 增加失败重试1次，或在失败时记录错误原因到工作表字段，下次优先处理。
- **预期收益**: 提高校准/处理成功率

## 🔵 问题 8: 分支「分支2」无后续处理

- **类别**: 异常处理
- **现状**: 在子流程「未命名子流程」中，分支条件不满足时直接结束，没有记录或通知。
- **建议**: 增加失败日志记录或计数器更新。
- **预期收益**: 便于排查问题

## 🔵 问题 9: 分支「分支2」无后续处理

- **类别**: 异常处理
- **现状**: 在子流程「未命名子流程」中，分支条件不满足时直接结束，没有记录或通知。
- **建议**: 增加失败日志记录或计数器更新。
- **预期收益**: 便于排查问题

## 🔵 问题 10: 子流程未命名（694796196436ebbd45a77437）

- **类别**: 可维护性
- **现状**: 子流程名称为「未命名子流程」，不便于理解和维护。
- **建议**: 重命名为能反映业务用途的名称。
- **预期收益**: 提高可维护性

## 🔵 问题 11: 子流程「未命名子流程」节点数过多（36）

- **类别**: 可维护性
- **现状**: 节点数 36 > 25，逻辑较复杂，不易维护。
- **建议**: 考虑拆分为多个子流程，每个子流程负责一个独立逻辑单元。
- **预期收益**: 降低维护难度

## 🔵 问题 12: 批次量为 100，需结合触发频率评估

- **类别**: 性能
- **现状**: 每次定时触发处理 100 条记录。如果总记录量远大于此，覆盖速度可能不够。
- **建议**: 确认触发间隔和待处理总量，评估是否需要增大批次量或提高触发频率。
- **预期收益**: 确保全量数据及时得到处理


---

# 「定时触发」优化方案

## 一、现有流程概述

```
工作流: 定时触发 (7节点)
  → [触发] 定时触发
  → [获取多条] 查询工作表 (挂售表-新逻辑) 筛选: 业务类型 属于(in) "外贸" 限制:100条
  → [子流程调用] 子流程 → 调子流程「未命名子流程」
  子流程: 未命名子流程 (36节点)
    → [触发] 子流程 (挂售表-新逻辑)
    → [获取单条] 代理 (芝麻代理) 筛选: 国家 属于(in) "国外" AND snk价格请求状态 属于(in) "正常"
    → [获取单条] 查询工作表 (比价数据) 筛选: global_sku_id 属于(in) [子流程].global_sku_id
    → [分支]  [条件互斥 2路]
```

## 二、优化方案总览

| # | 优化项 | 优先级 | 预期收益 | 实施难度 |
|---|--------|--------|----------|----------|
| 1 | 将「芝麻代理」的查询上移到主流程（子流程之前），通过参数传入子流程。 | 高 | 减少 99 次数据库查询 | 低-中 |
| 2 | 将「单尺码列表-非账户」的查询上移到主流程（子流程之前），通过参数传入子流程。 | 高 | 减少 99 次数据库查询 | 低-中 |
| 3 | 将「衣服单尺码列表数据」的查询上移到主流程（子流程之前），通过参数传入子流程。 | 高 | 减少 99 次数据库查询 | 低-中 |
| 4 | 增加品类/类型判断条件，改为条件互斥分支，只调用匹配的API。 | 高 | 减少约50%的外部API调用 | 低-中 |
| 5 | 改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。 | 中 | 避免因临时故障导致的数据丢失 | 中 |
| 6 | 改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。 | 中 | 避免因临时故障导致的数据丢失 | 中 |
| 7 | 增加失败重试1次，或在失败时记录错误原因到工作表字段，下次优先处理。 | 中 | 提高校准/处理成功率 | 中 |
| 8 | 增加失败日志记录或计数器更新。 | 低 | 便于排查问题 | 低 |
| 9 | 增加失败日志记录或计数器更新。 | 低 | 便于排查问题 | 低 |
| 10 | 重命名为能反映业务用途的名称。 | 低 | 提高可维护性 | 低 |
| 11 | 考虑拆分为多个子流程，每个子流程负责一个独立逻辑单元。 | 低 | 降低维护难度 | 低 |
| 12 | 确认触发间隔和待处理总量，评估是否需要增大批次量或提高触发频率。 | 低 | 确保全量数据及时得到处理 | 低 |

## 三、实施步骤

### 优先处理（高优先级）

**1. 「芝麻代理」在子流程中被重复查询**

现状：每条记录进入子流程都会查询一次「芝麻代理」（筛选条件不依赖当前记录），100条记录=100次相同查询。

方案：将「芝麻代理」的查询上移到主流程（子流程之前），通过参数传入子流程。

**2. 「单尺码列表-非账户」在子流程中被重复查询**

现状：每条记录进入子流程都会查询一次「单尺码列表-非账户」（筛选条件不依赖当前记录），100条记录=100次相同查询。

方案：将「单尺码列表-非账户」的查询上移到主流程（子流程之前），通过参数传入子流程。

**3. 「衣服单尺码列表数据」在子流程中被重复查询**

现状：每条记录进入子流程都会查询一次「衣服单尺码列表数据」（筛选条件不依赖当前记录），100条记录=100次相同查询。

方案：将「衣服单尺码列表数据」的查询上移到主流程（子流程之前），通过参数传入子流程。

**4. 并行分支中同时调用多个外部API（鞋子、衣服）**

现状：并行分支同时执行所有路径，但如果数据只属于其中一个类别，则另一个调用完全浪费。

方案：增加品类/类型判断条件，改为条件互斥分支，只调用匹配的API。

### 建议处理（中优先级）

**1. 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录**

现状：删除操作不可逆。如果上游条件判断错误（如外部API临时故障返回空数据），可能导致误删。

方案：改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。

**2. 在子流程「未命名子流程」中直接删除「挂售表-新逻辑」的记录**

现状：删除操作不可逆。如果上游条件判断错误（如外部API临时故障返回空数据），可能导致误删。

方案：改为状态标记（如"待确认下架"），增加连续失败计数器，多次失败后再执行真正删除。

**3. 2个外部API/表调用无重试机制**

现状：外部调用（鞋子、衣服）失败后直接跳过，无重试。

方案：增加失败重试1次，或在失败时记录错误原因到工作表字段，下次优先处理。

### 可选改进（低优先级）

**1. 分支「分支2」无后续处理**

方案：增加失败日志记录或计数器更新。

**2. 分支「分支2」无后续处理**

方案：增加失败日志记录或计数器更新。

**3. 子流程未命名（694796196436ebbd45a77437）**

方案：重命名为能反映业务用途的名称。

**4. 子流程「未命名子流程」节点数过多（36）**

方案：考虑拆分为多个子流程，每个子流程负责一个独立逻辑单元。

**5. 批次量为 100，需结合触发频率评估**

方案：确认触发间隔和待处理总量，评估是否需要增大批次量或提高触发频率。
